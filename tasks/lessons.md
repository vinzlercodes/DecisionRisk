# Lessons

- Keep `audit.md` current after every task so remaining implementation work and validation gaps are visible.
- Before finalizing implementation plans, cross-check the source hierarchy (`README.md`, `CONTEXT.md`, ADRs, `audit.md`, `tasks/todo.md`, demo docs, and the source-of-truth PDF when relevant) so the plan aligns with project language, boundaries, and current implementation truth.
